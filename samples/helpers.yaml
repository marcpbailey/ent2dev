# Implement a custom limit for missing device reporting
# Typically added to configuration.yaml

input_number:
  missing_devices_limit:
    name: Missing Devices Limit
    min: 1
    max: 100
    step: 1
    unit_of_measurement: devices
    initial: 30